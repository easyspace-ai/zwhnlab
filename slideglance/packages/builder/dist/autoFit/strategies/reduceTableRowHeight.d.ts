import type { BuilderNode } from "../../types.ts";
/**
 * Shrink `table.defaultRowHeight` and per-row heights.
 * @returns `true` when something changed.
 */
export declare function reduceTableRowHeight(node: BuilderNode, targetRatio: number): boolean;
//# sourceMappingURL=reduceTableRowHeight.d.ts.map