# Generated by `tauri-build` from `tauri.conf.json` + capabilities at every
# `cargo build`. The contents are deterministic given the inputs but shift
# between Tauri versions, so we keep them out of source control.
/gen/schemas/

# Tauri build emits target/ here when the bundler runs from this directory.
/target/
